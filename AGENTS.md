# AGENTS.md

## 1. 프로젝트 정의

이 프로젝트는 프레시매니저 유동판매 위치·시간 추천 서비스의 선행 단계인
서울시 공개데이터 기반 데이터 타당성 PoC다.

현재 목표는 모바일 앱이나 추천 화면을 만드는 것이 아니다.

현재 목표는 서울시 주요 121장소의 데이터를 안정적으로 수집하고,
장소별·시간대별 인구 변화, 미래 인구예측, 카드소비 기반 소비활동,
날씨정보를 비교할 수 있는 데이터 기반을 만드는 것이다.

---

## 2. 현재 검증 목표

1. 서울시 주요 121장소의 실시간 인구 데이터를 안정적으로 수집할 수 있는가
2. 장소별 현재 인구값과 미래 인구예측값을 모두 저장할 수 있는가
3. 같은 미래시점에 대한 예측값을 수집시점별로 보존할 수 있는가
4. 서울시 예측값과 후속 관측값을 비교할 수 있는가
5. 장소 유형과 요일·시간대에 따른 반복 패턴이 존재하는가
6. 인구 증가와 카드소비 기반 소비활동 변화가 함께 나타나는가
7. 수집된 데이터가 향후 프레시매니저 서비스 가설 검증에 활용 가능한가

---

## 3. 프로젝트 운영자

프로젝트 운영자와 최종 승인자는 PM 신동현이다.

운영자는 비개발자다.

Codex는 다음 원칙을 지켜야 한다.

- 새로운 개발 용어를 사용하면 쉬운 한국어 설명을 함께 제공한다.
- 실행 명령은 복사해서 바로 사용할 수 있게 작성한다.
- 파일을 수정하면 무엇을 왜 수정했는지 파일별로 설명한다.
- 오류가 발생하면 원인과 해결 순서를 한국어로 설명한다.
- 운영자가 직접 확인해야 할 작업을 별도로 구분한다.
- 구현 완료라고만 말하지 말고 검증 결과를 함께 제시한다.
- 확실하지 않은 API 필드나 동작을 추측해 구현하지 않는다.

---

## 4. 수집 범위와 분석 범위

### 수집 범위

서울시 주요 121장소 전체를 대상으로 한다.

### 1차 분석 범위

- 여의도
- 장소 유형별 대표 장소
- 데이터 품질 기준을 통과한 일부 장소

### 후속 분석 범위

121장소 전체 수집 안정성이 확인된 이후
장소 유형별·시간대별 비교분석으로 확대한다.

다음 두 범위를 혼동하지 않는다.

```text
수집 범위: 서울시 주요 121장소 전체
초기 구현 범위: 여의도 1장소 → 유형별 대표 3장소 → 시험용 10장소 → 121장소 1회 순차 수집
초기 분석 범위: 여의도 및 대표 장소
```

---

## 5. 공식 장소 목록 기준

수집 대상과 장소코드의 유일한 공식 기준파일은 다음과 같다.

```text
data/reference/seoul_121_places.csv
```

- XLSX 파일과 XLSX 보존 경로는 사용하지 않는다.
- 수집기와 Project Guard는 같은 공식 CSV만 장소 순회·코드 조회·검증 기준으로 사용한다.
- CSV는 Python 표준 라이브러리 `csv` 모듈로만 읽는다.
- `encoding="utf-8-sig"`를 사용해 UTF-8과 UTF-8 BOM을 모두 처리하며,
  BOM 존재 자체를 필수조건으로 요구하지 않는다.
- 공식 CSV 처리를 위해 `openpyxl`을 사용하거나 설치하지 않는다.
- 코드, 수집기, Project Guard와 검증 절차는 공식 CSV를 수정·보정·정렬·덮어쓰기·
  재인코딩하거나 다른 형식으로 변환하지 않는다.

현재 파일 상태는 다음과 같다.

- 공식 CSV 배치·정비 및 `main` 반영: 완료
- CSV 무결성 검증: 읽기 전용 재검증 완료
- EG-1 통과: PASS

기준 컬럼:

- CATEGORY
- NO
- AREA_CD
- AREA_NM
- ENG_NM

공식 CSV를 사용하기 전에는 반드시 다음을 읽기 전용으로 확인한다.

1. 지정 경로에 파일이 존재하는가
2. `utf-8-sig`로 UTF-8 또는 UTF-8 BOM 파일을 읽을 수 있는가
3. `CATEGORY`, `NO`, `AREA_CD`, `AREA_NM`, `ENG_NM`만 정확한 순서로 존재하는가
4. 데이터 행이 헤더를 제외하고 정확히 121개인가
5. `AREA_CD`가 비어 있지 않은가
6. `AREA_CD`가 중복되지 않는가
7. `AREA_NM`이 비어 있지 않은가
8. 여의도의 `AREA_CD`가 `POI072`인가
9. 분류별 건수를 보고하고 그 합계가 121인가
10. 장소코드를 자동 생성·변환·보정하지 않았고 검증 전후 CSV가 바뀌지 않았는가

검사에 실패하면 장소코드를 자동 보정하거나 수집을 계속하지 않고 PM에게 보고한다.

장소코드는 검증을 통과한 공식 CSV의 `AREA_CD`를 그대로 사용한다.

다음 행위를 금지한다.

- `POI001`부터 `POI121`까지 코드를 임의 생성
- 순번 `NO`를 장소코드로 사용
- 장소명을 추측해서 추가
- 존재하지 않는 코드를 자동 보정
- 유사한 장소명을 하나로 합침
- 공식 CSV를 쓰기 모드로 열거나 내용을 임의 수정·변환

여의도의 공식 장소코드는 다음과 같다.

```text
POI072
```

`POI`는 영문 대문자 `P`, `O`, `I`다.

---

## 6. PM 승인 원칙

검사 실행, 읽기 전용 확인, PASS·FAIL 판정 자체에는 PM 승인이 필요하지 않다.

다음 작업은 PM의 명시적 승인 없이 진행하지 않는다.

- 품질 게이트 통과 후 다음 구현 단계로의 전환
- 실제 서울시 API 호출
- 새로운 패키지 또는 라이브러리 설치
- 기준파일 생성·배치·교체·변경
- 데이터 저장구조 변경
- API 호출주기 결정 또는 변경
- 자동실행 구성 또는 변경
- GitHub main 브랜치 병합

새로운 판단이 필요하면 임의로 결정하지 않는다.

반드시 다음 형식으로 PM에게 보고한다.

1. 확인된 문제
2. 선택 가능한 방법
3. 각 방법의 장점과 단점
4. 권장안
5. PM 승인이 필요한 이유

---

## 7. 현재 허용된 구현 범위

- 검증된 공식 CSV의 121장소 목록 읽기
- 장소 목록 유효성 검사
- 여의도 1장소 API 호출
- 유형별 대표 3장소 시험수집
- 시험용 10장소 수집
- 121장소 1회 순차 수집
- 장소별 실시간 인구 현재값 저장
- 장소별 미래 인구예측 스냅샷 저장
- 지원 장소의 카드소비 기반 상권현황 저장
- 날씨 관측값과 예보값 저장
- 원본 JSON 저장
- 분석용 CSV 저장
- 수집 성공·실패 로그 저장
- 데이터 품질 검사
- 비개발자용 실행방법 문서화
- 호출한도와 처리시간 측정
- PM 승인 후 반복수집 준비

---

## 8. 현재 제외 범위

- 모바일 앱
- 웹 서비스 화면
- 지도 UI
- 프레시매니저 위치 추적
- 실제 판매량 수집
- 고객 개인정보 수집
- 개별 건물 또는 지하철 출구 추천
- 이동경로 최적화
- 자체 AI 예측 모델
- 머신러닝 학습
- 유료 데이터
- hy 내부 데이터 연동
- 대중교통·문화행사 데이터의 필수 연동
- 프로덕션 수준의 대규모 인프라
- 호출한도 확인 전 121장소 고빈도 자동수집
- PM 승인 없는 5분 단위 전체수집

---

## 9. 필수 구현 순서

Codex는 다음 순서를 건너뛰지 않는다.

```text
EG-0 문서 기준선
→ EG-1 장소 기준데이터 사전검증
→ EG-2 샘플 JSON 사전검증
→ EG-3 Project Guard 구현 및 자동 재검증
→ EG-4 여의도 1장소
→ EG-5 유형별 대표 3장소
→ EG-6 시험용 10장소
→ EG-7 121장소 1회 수집
→ EG-8 반복수집 주기 승인
```

- EG-0은 여섯 기준 문서의 구조·역할·규칙·현재 상태를 문서 검사로 확인한다.
- 현재 EG-0은 PM 승인으로 통과했다.
- EG-1은 Project Guard 구현 전에 공식 CSV 단일파일을 읽기 전용으로 사전검증한다.
- EG-2는 Project Guard 구현 전에 `data/samples/population_yeouido_sample.json`을
  네트워크 없이 읽기 전용으로 사전검증한다.
- 현재 공식 샘플은 `H-301`부터 `H-304`까지 PASS해 EG-2를 통과했다.
- 위 파일만 공식 여의도 실응답 샘플로 사용한다.
- `tests/fixtures/`는 결측 필드, 잘못된 JSON, 빈 예측 배열 등 오류 테스트
  입력에만 사용하며 공식 실응답 샘플을 이동·복사하지 않는다.
- EG-1과 EG-2에는 아직 존재하지 않는 Project Guard의 PASS를 요구하지 않는다.
- EG-3에서 문서, 공식 CSV와 샘플 JSON을 오프라인 Project Guard로 자동 재검증한다.
- EG-3까지는 실제 `.env` 생성, 실제 인증키 저장, 네트워크 연결과
  실제 서울시 API 호출을 금지하고 임시 설정과 가짜 키만 사용한다.
- 실제 `.env` 생성·인증키 저장과 최초 실제 API 호출은 EG-3 오프라인
  Project Guard 통과 후 PM 승인을 받아 EG-4 여의도 1장소 단계에서 진행한다.
- EG-4부터 EG-7까지도 일반 테스트와 승인된 실제 API 실행을 분리한다.
- 각 게이트 통과 후 다음 구현 단계로 전환하려면 PM 승인을 받는다.
- EG-8 통과는 반복주기 승인까지를 뜻하며 자동실행 구성 완료를 뜻하지 않는다.

Gate A·Gate B·Gate C는 데이터 PoC 판정 게이트다.
구현 준비도와 엔지니어링 품질을 판정하는 EG-0~EG-8과 혼용하지 않는다.

---

## 10. 배치 수집 원칙

서울시 API는 한 번 호출할 때 한 장소를 조회한다.

121장소 전체 수집은 다음 구조로 실행한다.

```text
공식 장소 목록 읽기
→ 장소 하나 호출
→ 원본 저장
→ 정규화 데이터 저장
→ 성공·실패 로그 기록
→ 다음 장소 호출
→ 121장소까지 반복
→ 회차 결과 요약
```

반드시 다음을 지킨다.

- 한 장소의 실패로 전체 회차를 중단하지 않는다.
- 실패 장소는 로그에 기록하고 다음 장소를 계속 처리한다.
- 전체 성공 건수와 실패 건수를 보고한다.
- 실패 장소 목록을 별도로 제공한다.
- 재시도는 무한 반복하지 않는다.
- 호출 사이 대기시간은 설정값으로 관리한다.
- 호출주기는 PM 승인 전 고정하지 않는다.

---

## 11. API 키 보안 규칙

서울시 API 키는 비밀정보로 취급한다.

- API 키는 `.env` 파일에서만 읽는다.
- API 키를 코드에 직접 작성하지 않는다.
- API 키를 README 또는 Markdown 문서에 작성하지 않는다.
- API 키를 콘솔에 출력하지 않는다.
- API 키를 오류 로그에 출력하지 않는다.
- API 키가 포함된 전체 URL을 출력하지 않는다.
- API 키를 Git에 커밋하지 않는다.
- `.env` 파일은 `.gitignore`에 포함한다.
- 공유용 파일에는 `.env.example`만 포함한다.
- 테스트에서 실제 API 키를 고정값으로 사용하지 않는다.

EG-3까지는 실제 `.env` 파일을 만들거나 실제 인증키를 저장하지 않는다.
EG-3 오프라인 Project Guard가 통과하고 PM이 EG-4 진입과 실제 호출을 승인한 뒤에만
EG-4 여의도 1장소 단계에서 `.env`를 생성하고 실제 인증키를 저장한다.

API 요청 주소를 로그에 기록할 때 인증키를 마스킹한다.

```text
http://openapi.seoul.go.kr:8088/********/json/...
```

---

## 12. 원본 데이터 보존 규칙

서울시에서 받은 원본 응답은 수정하지 않는다.

- 원본 JSON을 그대로 저장한다.
- 기존 원본 파일을 덮어쓰지 않는다.
- 호출할 때마다 새로운 파일을 만든다.
- 파일명에 장소코드와 요청시각을 포함한다.
- API 응답 필드명을 임의로 수정하지 않는다.
- 숫자가 문자열로 와도 원본에서는 변환하지 않는다.
- 값이 이상해 보여도 원본에서 삭제하지 않는다.
- 결측값을 0으로 바꾸지 않는다.
- 데이터 오류 여부는 별도 로그에 기록한다.

원본 파일명 예:

```text
POI072_20260716_200000.json
```

분석용 CSV에서만 숫자와 날짜 형식을 변환할 수 있다.

---

## 13. 인구 데이터 규칙

현재 인구 데이터에는 실제 API 응답에 존재하는 필드를 저장한다.

최소 확인 대상:

- AREA_NM
- AREA_CD
- AREA_CONGEST_LVL
- AREA_CONGEST_MSG
- AREA_PPLTN_MIN
- AREA_PPLTN_MAX
- MALE_PPLTN_RATE
- FEMALE_PPLTN_RATE
- PPLTN_RATE_0
- PPLTN_RATE_10
- PPLTN_RATE_20
- PPLTN_RATE_30
- PPLTN_RATE_40
- PPLTN_RATE_50
- PPLTN_RATE_60
- PPLTN_RATE_70
- RESNT_PPLTN_RATE
- NON_RESNT_PPLTN_RATE
- REPLACE_YN
- PPLTN_TIME
- FCST_YN

필드명과 응답 구조는 공식 샘플을 포함한 실제 API 응답을 구현 기준으로 사용한다.
예상 필드명과 실제 필드명이 다르면 실제 응답을 우선한다.

필드 차이는 `docs/data/FIELD_DICTIONARY.md`에 기록한다.

---

## 14. 예측 데이터 규칙

미래 예측값은 최신값 하나만 남기면 안 된다.

반드시 다음 시각을 구분한다.

- API 요청시각
- 예측 스냅샷 시각
- 예측 대상시각
- 후속 관측시각

같은 미래 대상시각의 예측이 여러 번 수집돼도
기존 예측값을 덮어쓰지 않는다.

최소 저장 대상:

- AREA_CD
- FCST_TIME
- FCST_CONGEST_LVL
- FCST_PPLTN_MIN
- FCST_PPLTN_MAX
- 요청시각
- 예측 스냅샷 시각

공식 예측 발행시각 필드가 확인되지 않으면 임의로 만들지 않는다.

---

## 15. 날씨 데이터 규칙

날씨 관측값과 날씨 예보값을 분리한다.

- 실제 관측값과 미래 예보값을 같은 CSV에 섞지 않는다.
- 예보에는 예보 발행시각과 예보 대상시각을 저장한다.
- 예측 평가에는 당시 이용 가능했던 예보값만 사용한다.
- 이후 확인된 실제 날씨를 과거 예측 입력값으로 사용하지 않는다.
- 실제 날씨는 사후 원인 분석과 예보 오차 검증에만 사용한다.
- 예보값이 없다고 실제 관측값으로 대체하지 않는다.

---

## 16. 상권현황 데이터 규칙

실시간 상권현황은 카드소비 기반 소비활동 대리변수다.

다음처럼 표현하지 않는다.

- 실제 야쿠르트 매출
- 프레시매니저 판매실적
- 실제 전체 소비금액
- 구매전환율

다음처럼 표현한다.

- 카드소비 기반 소비활동 대리변수
- 상권 활동단계
- 일반 소비활동 변화 신호

상권현황은 121장소 전체에서 지원되지 않을 수 있다.

지원하지 않는 장소와 일시적인 결측을 구분한다.

- `not_supported`: 해당 장소가 지원대상이 아님
- `missing`: 지원대상이지만 해당 시점 데이터가 없음

둘 다 `한산` 또는 `0`으로 바꾸지 않는다.

---

## 17. 최소 수집 메타데이터

1인 PoC에서는 다음 최소 메타데이터를 사용한다.

- request_id
- endpoint_name
- requested_at
- http_status
- area_code
- collection_status
- raw_file_path
- parser_version

`endpoint_name`에는 실제 URL이 아니라 `citydata_ppltn`과 같은 논리적 이름을 저장한다.
`raw_file_path`에는 원본 JSON 내용이 아니라 저장된 원본 파일 경로를 기록한다.

필요성이 확인되지 않은 메타데이터를 임의로 추가하지 않는다.

---

## 18. 호출주기 결정 원칙

121장소 전체 반복수집 주기는 사전에 확정하지 않는다.

다음 결과를 확인한 뒤 PM이 승인한다.

- API 일일 호출한도
- 121장소 1회 수집 소요시간
- 장소별 데이터 실제 갱신주기
- 호출 성공률과 실패율
- 재시도에 따른 추가 호출량
- 운영 컴퓨터의 장기 실행 안정성
- 분석에 필요한 최소 시간해상도

`5분마다 121장소 전체 호출`을 기본값으로 가정하지 않는다.

---

## 19. 코딩 원칙

- Python을 기본 언어로 사용한다.
- 가능한 경우 Python 표준 라이브러리를 우선한다.
- 불필요한 외부 라이브러리를 추가하지 않는다.
- 요청받지 않은 리팩터링을 하지 않는다.
- 오류를 숨기지 않는다.
- 광범위한 예외처리로 모든 오류를 무시하지 않는다.
- 비개발자가 실행할 수 있도록 명령을 단순하게 유지한다.
- 실제 응답에 없는 필드를 추측해서 만들지 않는다.
- 코드 변경과 문서 변경을 함께 관리한다.

---

## 20. Project Guard 검사 원칙

`docs/testing/PROJECT_GUARD_SPEC.md`는 검사 ID, 상태값, 판정과 종료 코드의
유일한 기준이다. 다른 문서에서 검사 ID를 새로 정의하지 않는다.

EG-3에서 Project Guard가 구현된 이후에는 모든 구현 작업 후 현재 게이트에
적용되는 필수검사를 실행한다.

문서만 수정하는 작업은 구현된 Project Guard 실행 대상이 아니다.
문서 전용 작업에서는 Python 기반 Project Guard를 새로 만들거나 실행하지 않고
다음을 확인한다.

- 코드 블록 정상 종료와 Markdown 제목 구조
- 프로젝트 목표, 수집 범위, 분석 범위의 일치
- 공식 CSV 경로와 배치·검증·EG-1 상태의 일치
- 검사 ID 중복·누락 여부
- 품질 게이트 순환 여부와 단계적 구현 순서의 일치
- Gate A·B·C와 EG-* 명칭의 구분
- 실제 API 호출과 일반 테스트의 분리
- 호출주기 결정 원칙의 일치
- 상권 상태와 API 키 보안 규칙의 일치
- 원본·예측·날씨 보존 규칙의 일치
- 완료·미완료 상태와 여섯 문서 사이의 충돌 여부

구체적인 자동검사 항목은 `docs/testing/PROJECT_GUARD_SPEC.md`만 따른다.
실행 결과는 `docs/testing/PROJECT_GUARD_REPORT_TEMPLATE.md`에 기록한다.

`scripts/project_guard_check.py`가 만들어진 이후에는
모든 구현 작업 종료 전에 다음 명령을 실행한다.

```bash
python3 scripts/project_guard_check.py
```

검사에 실패한 상태로 완료 보고하지 않는다.

---

## 21. 실데이터 호출 원칙

일반 Project Guard와 일반 테스트에서는 네트워크 연결과 실제 서울시 API 호출을 금지한다.

EG-3 오프라인 Project Guard가 통과하기 전에는 실제 인증키를 저장하거나
실제 서울시 API를 호출하지 않는다. 실제 호출은 EG-3 통과 후 PM이
EG-4 진입과 여의도 1장소 호출을 승인한 경우에만 일반 테스트와 분리된
수동 점검 또는 스모크 실행으로 수행한다.
일반 테스트는 저장된 샘플 JSON 또는 가짜 응답만 사용한다.

---

## 22. 변경 보고 형식

### 작업 요약

무엇을 구현했는지 설명한다.

### 변경 파일

각 파일별 변경내용과 이유를 설명한다.

### 실행 명령

운영자가 복사해서 실행할 명령을 제공한다.

### Project Guard 결과

실행한 검사와 성공·실패 결과를 제공한다.

### 수집 결과

- 전체 대상 장소 수
- 성공 장소 수
- 실패 장소 수
- 실패 장소 목록
- 총 소요시간

### PM 확인사항

PM이 직접 확인하거나 승인해야 할 내용을 구분한다.

### 남은 위험

아직 해결되지 않은 오류, 결측, API 제약을 설명한다.

### 범위 준수

요청 범위 밖 변경이 없는지 확인한다.

---

## 23. 완료 정의

구현 작업은 현재 EG에 적용되는 다음 조건을 모두 만족해야 완료로 보고한다.
후속 EG에서만 적용되는 조건을 현재 단계의 선행 완료조건으로 요구하지 않는다.
단계별 세부조건은 `docs/testing/QUALITY_GATES.md`를 따른다.

- 요청받은 범위만 구현함
- 검증된 공식 CSV를 기준으로 사용함
- 임의의 장소코드를 생성하지 않음
- 관련 문서를 업데이트함
- Project Guard 검사를 실행함
- Project Guard 검사에 통과함
- API 키가 노출되지 않음
- 원본 데이터가 보존됨
- 한 장소 실패가 전체 실패로 이어지지 않음
- 성공·실패 결과가 보고됨
- 실행방법이 한국어로 설명됨
- PM 확인사항이 구분됨
- 남은 위험이 숨겨지지 않음

문서 전용 작업은 다음 조건을 모두 만족해야 완료로 보고한다.

- 요청받은 문서만 수정함
- Markdown 제목 구조를 확인함
- `AGENTS.md`와 `README.md`의 핵심 규칙이 일치함
- 실제 파일 상태와 문서의 완료·미완료 표현이 일치함
- 구현된 Project Guard 실행 대신 문서 일관성 검사를 수행함
- 코드 블록, 제목, 검사 ID, 품질 게이트 순환을 확인함
- PM 확인사항과 남은 위험을 보고함

PM의 최종 승인 전에는 main 브랜치 병합 또는
최종 완료 상태로 확정하지 않는다.

## 24. 코드 및 Project Guard 규칙 문서

Codex는 코드, 테스트, 설정 또는 Project Guard 작업을 시작하기 전에
다음 문서를 반드시 읽어야 한다.

- `docs/rules/CODING_RULES.md`
- `docs/rules/GIT_WORKFLOW.md`
- `docs/rules/SECURITY_RULES.md`
- `docs/testing/PROJECT_GUARD_SPEC.md`
- `docs/testing/QUALITY_GATES.md`
- `docs/testing/PROJECT_GUARD_REPORT_TEMPLATE.md`

각 문서의 역할은 다음과 같다.

| 문서 | 역할 |
|---|---|
| `CODING_RULES.md` | 코드 구조, 오류처리, 보안, 저장 및 테스트 작성 규칙 |
| `GIT_WORKFLOW.md` | Issue, Branch, Worktree, Commit, Pull Request, Review, Merge 운영 규칙 |
| `SECURITY_RULES.md` | API 키, 로그, 민감정보, GitHub 및 외부 공유 보안 규칙 |
| `PROJECT_GUARD_SPEC.md` | 자동검사 항목과 검사 ID, PASS·FAIL·WARN·SKIP 판정, 종료 코드의 유일한 기준 |
| `QUALITY_GATES.md` | EG-0~EG-8의 순서, 단계별 통과조건과 다음 단계 진입조건 |
| `PROJECT_GUARD_REPORT_TEMPLATE.md` | `PROJECT_GUARD_SPEC.md`에 정의된 검사 결과와 PM 확인사항의 기록 형식 |

`GIT_WORKFLOW.md`와 `SECURITY_RULES.md`는 필수 열람 문서로 적용한다.
두 문서의 Project Guard 자동검사 대상 편입은 EG-3 후속 Issue에서 반영한다.

데이터 수집·필드·분석 관련 작업을 시작하기 전에는 다음 문서를 반드시 읽는다.

| 문서 | 역할 |
|---|---|
| `docs/rules/DATA_COLLECTION_RULES.md` | 데이터 수집, 원본 보존, 예측·날씨 분리와 결측 처리 규칙 |
| `docs/data/FIELD_DICTIONARY.md` | 원본·정규화·메타데이터·파생필드의 정의와 검증 상태 |
| `docs/analysis/ANALYSIS_PLAN.md` | 분석 질문, 기준선, 평가기간과 Gate A·B·C 판정 계획 |

세 문서의 Project Guard 자동검사 대상 편입은 EG-3 후속 Issue에서 반영한다.

Codex는 작업 시작 전에 다음을 확인한다.

1. 현재 작업이 어느 품질 게이트에 해당하는가
2. 이전 게이트가 통과됐는가
3. 이번 작업에 적용되는 Project Guard 검사 ID는 무엇인가
4. 이번 작업에서 생성하거나 수정할 파일은 무엇인가
5. PM 승인 없이 변경할 수 없는 사항이 있는가
6. 실제 API 호출이 필요한 작업인가
7. 실제 API 호출에 대한 PM 승인이 있는가

현재 품질 게이트를 통과하지 않은 상태에서
다음 단계의 코드를 임의로 구현하지 않는다.

아직 구현되지 않은 기능에 대한 Project Guard 검사는 `SKIP`으로 표시할 수 있다.

단, 구현 완료로 보고한 기능에 필요한 검사를 `SKIP`으로 처리하면 안 된다.

문서 간 규칙이 충돌할 경우 다음 우선순위를 적용한다.

1. PM의 최신 명시적 지시
2. `AGENTS.md`
3. `requirements-definition-freshmanager-poc-v0.4.md`
4. `docs/rules/CODING_RULES.md`
5. `docs/testing/PROJECT_GUARD_SPEC.md`
6. `docs/testing/QUALITY_GATES.md`
7. `README.md`
8. 그 밖의 운영 및 참고 문서

충돌을 발견하면 Codex가 임의로 선택하거나 수정하지 않는다.

다음 형식으로 PM에게 보고한다.

1. 충돌한 문서와 섹션
2. 서로 다른 규칙
3. 구현에 미치는 영향
4. 선택 가능한 수정안
5. 권장안
6. PM 승인이 필요한 이유

## 25. GitHub Issue·Pull Request 템플릿

- `.github/ISSUE_TEMPLATE/task.md`: 모든 변경 작업의 Issue 작성에 사용한다.
- `.github/pull_request_template.md`: 모든 Pull Request 작성과 검토에 사용한다.

변경 작업은 Issue에서 PM의 구현계획 승인을 받은 뒤 시작하고,
Pull Request는 해당 Issue를 연결하며 PM의 최종 Merge 승인을 받는다.
