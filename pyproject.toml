[project]
name = "freshmanager-api"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "fastapi==0.141.1",
    "pydantic==2.13.4",
    "uvicorn==0.52.0",
]

[tool.vercel]
entrypoint = "apps.api.main:app"
